// Generated by CoffeeScript 1.10.0
var Brackets;

Brackets = (function() {
  function Brackets(params) {
    if (params.type === 'round-robin') {
      this.matches = params.entrants;
    }
    if (params.type === 'single-elimination') {
      this.matches = params.entrants;
    }
    if (params.type === 'double-elimination') {
      this.matches = params.entrants;
    }
  }

  return Brackets;

})();

module.exports = Brackets;
